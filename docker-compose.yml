services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: tank
      POSTGRES_PASSWORD: tank
      POSTGRES_DB: tank
    ports:
      - "5432:5432"
    volumes:
      - tank_postgres:/var/lib/postgresql/data

volumes:
  tank_postgres:
